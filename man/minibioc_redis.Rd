% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minibioc_redis.R
\name{minibioc_redis}
\alias{minibioc_redis}
\title{Create a minibioc repository using Redis worker queue}
\usage{
minibioc_redis(
  bioc_version = BiocManager::version(),
  image_name = "bioconductor_docker",
  volume_mount_path = minibioc_base_dir(),
  cloud_id = c("local", "gcp", "azure"),
  build = c("_software", "_update", "_timings"),
  depth0 = FALSE,
  dry.run = TRUE,
  ultimate_pkg = character(),
  exclude_pkgs = character()
)
}
\arguments{
\item{bioc_version}{\code{character(1)} The version of the repository/Bioconductor.}

\item{image_name}{\code{character(1)} The name of the Docker image used for building.}

\item{volume_mount_path}{\code{character(1)} The base directory where the repository
is mounted/created.}

\item{cloud_id}{\code{character(1)} The cloud provider to use, one of \code{"local"},
\code{"gcp"}, or \code{"azure"}.}

\item{build}{\code{character(1)} The build mode, one of \code{"_software"}, \code{"_update"},
or \code{"_timings"}.}

\item{depth0}{\code{logical(1)} Whether to only install/build packages with zero dependencies.}

\item{dry.run}{\code{logical(1)} Whether to run a dry run.}

\item{ultimate_pkg}{\code{character()} The ultimate package(s) to target.}

\item{exclude_pkgs}{\code{character()} Packages to exclude from the dependency list.}
}
\description{
This function builds binaries for the given package dependency
graph using RedisParam to parallelize builds across workers.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
minibioc_redis(
    build = "_software",
    ultimate_pkg = "IRanges",
    exclude_pkgs = c("canceR", "ChemmineOB", "flowCore")
)
\dontshow{\}) # examplesIf}
}
