% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minibioc.R
\name{minibioc_local}
\alias{minibioc_local}
\alias{minibioc_clean}
\title{Create a minibioc local repository}
\usage{
minibioc_local(
  bioc_version = BiocManager::version(),
  base_repo_dir = minibioc_base_dir(),
  build = c("_software", "_update", "_timings"),
  depth0 = FALSE,
  dry.run = TRUE,
  ultimate_pkg = character(),
  exclude_pkgs = character()
)

minibioc_clean(
  version = BiocManager::version(),
  bin_path = local_bin_repo(),
  dry.run = TRUE
)
}
\arguments{
\item{bioc_version}{\code{character(1)} The version of the repository/Bioconductor
to create. By default, it is the value of \code{BiocManager::version()}.}

\item{base_repo_dir}{\code{character(1)} The base directory where the CRAN-like
repository will be created. By default, it is the value of \code{minibioc_base_dir()}.}

\item{build}{\code{character(1)} The build mode, one of \code{"_software"}, \code{"_update"},
or \code{"_timings"}.}

\item{depth0}{\code{logical(1)} Whether to only install/build packages with zero
dependencies (plus the \code{ultimate_pkg} if specified). By default, \code{FALSE}.}

\item{dry.run}{\code{logical(1)} Whether to run a dry run (list files to delete
instead of deleting).}

\item{ultimate_pkg}{\code{character()} The last package in the queue to start
from especially useful after interrupted builds.}

\item{exclude_pkgs}{\code{character()} Packages to exclude from the dependency
list.}

\item{version}{\code{character(1)} The version of the Bioconductor repository}

\item{bin_path}{\code{character(1)} The directory path of the local binary
repository}
}
\description{
Create a minibioc local repository
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
## Point to source package directories
source_base_dir <- "~/bioc"
bioc_sub_pkgs <- file.path(
    source_base_dir, c(
        "SummarizedExperiment", "Biobase", "BiocBaseUtils",
        "BiocGenerics", "DelayedArray", "GenomicRanges",
        "IRanges", "S4Vectors"
    )
)

## Set up git credentials for private repositories if needed
gitcreds::gitcreds_set()

## Git sync local source repositories
ReleaseLaunch::update_local_repos(
    repos_dir = source_base_dir, org = "Bioconductor"
)

## specify minibioc base repository directory
repo_dir <- minibioc_base_dir()

## Build local source package tarballs in the local source repository
for (pkg in bioc_sub_pkgs) {
    build_source_package(
        pkg = pkg,
        lib_path = local_library(base_repo_dir = repo_dir),
        bin_path = local_src_repo(base_repo_dir = repo_dir),
        log_path = local_src_log(base_dir = repo_dir)
    )
}

## Build local binaries and manage the local CRAN-like repository
minibioc_local(
    base_repo_dir = repo_dir,
    build = "_software",
    depth0 = TRUE,
    dry.run = TRUE,
    ultimate_pkg = "IRanges",
    exclude_pkgs = c("canceR", "ChemmineOB", "flowCore")
)
\dontshow{\}) # examplesIf}
}
