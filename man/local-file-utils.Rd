% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{local-file-utils}
\alias{local-file-utils}
\alias{local_type_area}
\alias{local_bin_repo}
\alias{local_src_repo}
\alias{local_src_log}
\alias{local_bin_log}
\alias{local_library}
\title{Helper functions to manage local repository paths and directories}
\usage{
local_type_area(
  base_repo_dir = minibioc_base_dir(),
  version = BiocManager::version(),
  type = getOption("pkgType"),
  uri = FALSE,
  image_name = "bioconductor_docker"
)

local_bin_repo(
  base_repo_dir = minibioc_base_dir(),
  version = BiocManager::version(),
  contrib.url = TRUE,
  uri = FALSE,
  ...
)

local_src_repo(
  base_repo_dir = minibioc_base_dir(),
  version = BiocManager::version(),
  contrib.url = TRUE,
  include.Meta = FALSE,
  uri = FALSE,
  ...
)

local_src_log(base_dir = minibioc_base_dir(), version = BiocManager::version())

local_bin_log(base_dir = minibioc_base_dir(), version = BiocManager::version())

local_library(
  base_repo_dir = minibioc_base_dir(),
  version = BiocManager::version()
)
}
\arguments{
\item{base_repo_dir}{\code{character(1)} The base directory of the repository.}

\item{version}{\code{character(1)} The version of the repository.}

\item{uri}{\code{logical(1)} Whether to prepend 'file://'.}

\item{contrib.url}{\code{logical(1)} Whether to append the standard R contrib
path.}

\item{...}{Additional arguments passed to \code{local_type_area}.}

\item{include.Meta}{\code{logical(1)} Whether to include the Meta directory in the
path.}

\item{base_dir}{\code{character(1)} The base repository directory.}
}
\description{
These functions are used to manage the local repository paths
and directories for both source and binary packages. They provide a
consistent way to construct paths to the local repositories, logs, and
library directories based on the base repository directory and Bioconductor
version. The \code{local_type_area} function is a helper function that
constructs the path to either the source or binary repository based on the
specified type. The \code{local_bin_repo} and \code{local_src_repo} functions use
\code{local_type_area} to get the paths to the binary and source repositories,
respectively. The \code{local_src_log}, \code{local_bin_log}, and \code{local_library}
functions create and return paths for logs and library directories,
ensuring that they exist before returning the path.
}
\details{
\preformatted{
- local_type_area:   Helper function to construct path based on Bioc version,
                     type (source or binary), image_name (for binaries), etc.
- local_bin_repo:    Constructs path to the local binary repository.
- local_src_repo:    Constructs path to the local source repository.
- local_src_log:     Constructs path to the local source build logs.
- local_bin_log:     Constructs path to the local binary build logs.
- local_library:     Constructs path to the local library directory.
}
}
