% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minibioc_install.R
\name{minibioc_single}
\alias{minibioc_single}
\alias{install_build_binary}
\alias{build_source_package}
\title{Install and create binaries for R packages}
\usage{
install_build_binary(
  pkg,
  dry.run,
  lib_path,
  bin_path = local_bin_repo(),
  log_path
)

build_source_package(pkg, lib_path, bin_path, log_path)
}
\arguments{
\item{pkg}{\code{character(1)} name of R or Bioconductor package.}

\item{lib_path}{\code{character(1)} path where R package libraries are stored.}

\item{bin_path}{\code{character(1)} path where R package binaries are stored.}

\item{log_path}{\code{character(1)} path where R package binary build logs are
stored.}
}
\value{
\code{install_build_binary()} returns invisibly
}
\description{
Generalized from \code{BiocKubeInstall}. This function installs a
single R package and creates its binary file for installation.
}
\details{
The package given by \code{pkg} is installed in the given library path
\code{lib_path}, and the binaries are created in the \code{bin_path}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
ver_name <-
    BiocManager:::.version_field("BiocStatus") |> as.character()
repo_file <- file.path(
    local_src_repo(contrib.url = FALSE), "REPOSITORY"
)
if (!file.exists(repo_file))
    download.file(
        url = glue::glue(
            "https://bioconductor.org/packages/{ver_name}/bioc/REPOSITORY"
        ),
        destfile = repo_file
    )
## Point to source package directories
source_base_dir <- "~/bioc"
bioc_sub_pkgs <- file.path(
    source_base_dir, c(
        "SummarizedExperiment", "Biobase", "BiocBaseUtils",
        "BiocGenerics", "DelayedArray", "GenomicRanges",
        "IRanges", "S4Vectors"
    )
)

## Install local binaries for a single package
for (pkg in bioc_sub_pkgs) {
    install_build_binary(
        pkg = pkg,
        dry.run = FALSE,
        lib_path = local_library(),
        bin_path = local_bin_repo(),
        log_path = local_bin_log()
    )
}

## Create PACKAGES, PACKAGES.gz, PACAKGES.rds for the local binary repository
tools::write_PACKAGES(local_bin_repo(), addFiles = TRUE, verbose = TRUE)

## Create PACKAGES, PACKAGES.gz, PACAKGES.rds for the local source repository
tools::write_PACKAGES(local_src_repo(), addFiles = TRUE, verbose = TRUE)
\dontshow{\}) # examplesIf}
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
build_source_package(
    pkg = "~/bioc/BiocParallel",
    lib_path = local_library(),
    bin_path = local_src_repo(),
    log_path = local_src_log()
)
\dontshow{\}) # examplesIf}
}
